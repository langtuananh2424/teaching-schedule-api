spring.application.name=teaching-schedule-api

# By default, the "local" profile is active.
# This will be overridden by an environment variable when running with Docker.
spring.profiles.active=local

# --- Common Configuration ---
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

app.jwt.secret=ThuyLoiUniversitySecretKeyForTeachingScheduleAPIProjectThisKeyShouldBeLongAndComplex
app.jwt.expiration-ms=86400000

server.address=0.0.0.0
